import Foundation

class YearInViewModel {
	
	func fetchYearInViewData() {
		// Steps
		// 1. Check if response data is availabe in UserDefaults
		// a. if available: return response data.
		// b. otherwise fetch from endpoint
		// i. Decode into YearInViewResponse
		// ii. Handle errors with an alert
		// iii. Save response into UserDefaults so that we don't have to fetch it again after first time.
	}
	
}
